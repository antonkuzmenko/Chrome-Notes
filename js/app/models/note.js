// Generated by CoffeeScript 1.6.2
(function() {
  var Note, app;

  app = window.app;

  Note = Backbone.Model.extend({
    defaults: function() {
      return {
        title: '',
        body: ''
      };
    },
    type: 'note',
    initialize: function() {
      var _ref;

      if ((_ref = this.id) == null) {
        this.id = app.Iterator.note.next();
      }
      new app.View.Note({
        model: this
      });
      return this.listenEvents();
    },
    listenEvents: function() {
      this.on('all', function(event) {
        return console.log(event, 'Note event');
      });
      this.on('remove', this.destroy, this);
      return this.on('remove', this.clear, this);
    }
  });

  Note.prototype.belongsTo = function() {
    return {
      folder: app.Collection.Folders
    };
  };

  app.Model.Note = Note;

}).call(this);
